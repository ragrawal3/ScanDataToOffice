import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InvoiceSelectionPage } from './invoice-selection.page';

describe('InvoiceSelectionPage', () => {
  let component: InvoiceSelectionPage;
  let fixture: ComponentFixture<InvoiceSelectionPage>;

  beforeEach(async() => {
    fixture = TestBed.createComponent(InvoiceSelectionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });
});
